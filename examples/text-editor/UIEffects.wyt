type UIEffects
  effect PaintUI >= {system.FFI}
  effect SetUILookAndFeel >= {system.FFI}
  effect ShowDialog >= {system.FFI}
  effect GetMessageValue >= {system.FFI}
  effect ReadTextArea >= {system.FFI}
