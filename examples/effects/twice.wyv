module def twice(file:effects.File)

// small hack as the Unit -> {E} Unit notation doesn't work yet
type Function
    effect E
    def apply(): {this.E} Unit

def invokeTwice[effect E](f: Function[{E}]):Unit
   f()
   f()

//invokeTwice[file.Read]( () -> file.read() )
