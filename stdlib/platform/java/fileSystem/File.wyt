import fileSystem.Reader
import fileSystem.Writer
import fileSystem.RandomAccessFile
resource type File
    effect read
    effect write
    effect open
    effect close
    def makeReader(): {this.open} Reader
    def makeWriter(): {this.open} Writer
    def makeAppender(): {this.open} Writer
    def makeRandomAccessFile(mode : String): {this.open} RandomAccessFile