import polymorphicEffects.Logger

module def reversePlugin(name : String)
  val logger : Logger =  pureLogger()

  def setLogger(newLogger : Logger) : Unit
    logger = newLogger
    
  def run(s : String) : String
    val t = s.reverse()
    logger.append(name + s + t)
    t
