type Logger
  type T
  def append(s : String) : {} Unit

val myLogger : Logger = new
  type T = Int
  def append(s : String) : {} Unit
    s

val u = new
  effect E = {system.FFI}

// Cannot refine abstract type
type S = myLogger.T[{u.E}]

"abc"