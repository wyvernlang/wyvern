import metadata wyvern.IfTSL2

// TODO: put this in the standard prelude, and allow it be called 'if'
val iffelsARG = (cond:Boolean, tt:IfTSL2.FnExpr, ff:IfTSL2.FnExpr) => IfTSL2.doif(cond, tt, ff)

val x = -5
val y : Int = iff (x>0) {x} els {-x}
/*val z : Int = iff (x>0)
    thn
        x
    els
        -x*/
y

// design question: should we just replace the whole ~ syntax with a scheme more like what's above?