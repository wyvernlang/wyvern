module def network0C(): effects.NetworkType0C
effect send = {}
effect receive = {}
var data: String = ""
val n: Int = 3
def sendData(sd: String): {} Unit
    data = sd
def receiveData(): {receive} String
    data