module def network06(stdout : Stdout)// : NetworkType06
effect send = stdout
effect receive = {}
def sendData(data : String) : {} Unit
    stdout.print("data sent: ")
    stdout.print(data)
    stdout.print("\n")
def receiveData() : {} Unit
    stdout.print("data received\n")