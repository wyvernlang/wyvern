module def network0E(stdout : Stdout) : effects.NetworkType
effect send = {stdout.hi}
effect receive = {}
def sendData(data : String) : {} Unit
    stdout.print("data sent: ")
    stdout.print(data)
    stdout.print("\n")
def receiveData() : {receive} Unit
    stdout.print("data received\n")