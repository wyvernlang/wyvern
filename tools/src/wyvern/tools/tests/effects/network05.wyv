module def network05(stdout : Stdout) //: NetworkType05
effect send = {}
effect receive = {}
def sendData(data : String) : gibberish Unit
    stdout.print("data sent: ")
    stdout.print(data)
    stdout.print("\n")
def receiveData() : {} Unit
    stdout.print("data received\n")