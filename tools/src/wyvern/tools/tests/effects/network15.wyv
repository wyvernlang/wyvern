module def network15(): effects.NetworkType
effect send = {something}
effect receive = {}
var data: String = ""
def sendData(sd: String): {} Unit
    data = sd
def receiveData(): {receive} Unit
    data